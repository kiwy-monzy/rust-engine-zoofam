// DEPRECATED — the old @gateway/module-commerce package has been split into
// @gateway/module-erp and @gateway/module-crm. This file is intentionally
// empty so the bundler does not try to include it.
export {};