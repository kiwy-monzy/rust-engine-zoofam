// DEPRECATED — this module has been renamed to `erp`.
// All types live in `models::erp` now. `models::commerce` is kept as a
// re-export shim in `models/src/lib.rs` for backward compatibility.
// This file is intentionally empty so it is not picked up by the compiler.