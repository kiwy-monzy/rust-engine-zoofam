-- Zoho Commerce complete — DEPRECATED (Postgres).
-- Zoho-style tables are now part of the unified commerce_erp (030) and
-- crm_core (031) migrations. This folder is kept only so the migration
-- count and order stay stable. It is a no-op marker that diesel_migrations
-- still recognises as a valid migration.
SELECT 1;